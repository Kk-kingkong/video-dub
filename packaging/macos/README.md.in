# LocalTube Dub Engine __VERSION__

双击安装时默认绑定 Chrome 商店扩展 ID：`__EXTENSION_ID__`。

## 第一次安装

1. 先安装 LocalTube Dub Chrome 扩展。
2. 双击 `Install LocalTube Dub Engine.command`。
3. 安装完成后完全退出并重新打开 Chrome。
4. 在扩展弹窗或 YouTube 面板中点击“检查 Engine”。

安装器会把完整的私有运行时复制到 `~/Library/Application Support/LocalTube Dub/engine-runtime`，其中已包含 Python、yt-dlp、ffmpeg、Kokoro 运行时、Microsoft 自然在线语音依赖和 Native Messaging Host。Engine 在翻译或配音时按需启动，空闲 5 分钟后退出；升级会移除旧版登录自启动。主 Engine 安装不需要 Homebrew、预装 Python、pip、编译器或额外终端命令。Kokoro 模型不会随安装包下载，用户只在扩展中明确选择“安装本地模型”后才会下载它。只有用户主动选择“自然在线”时，待朗读字幕才会发送给 Microsoft。

## 加载已解压的扩展或 ZIP 测试包

Chrome 为已解压扩展分配的 ID 可能与商店 ID 不同。打开扩展安装说明或 `chrome://extensions`，复制 LocalTube Dub 的实际 ID；在此解压后的 Engine 安装包目录打开终端，将下面的 `YOUR_EXTENSION_ID` 替换后运行：

```bash
bash "./Install LocalTube Dub Engine.command" YOUR_EXTENSION_ID
```

已经安装过 Engine 时，也可以直接重新绑定当前扩展：

```bash
bash "$HOME/Library/Application Support/LocalTube Dub/engine-runtime/companion/install_native_host_macos.sh" YOUR_EXTENSION_ID
```

出现 `Access to the specified native messaging host is forbidden` 表示扩展未获此 Engine 的访问授权；重复重启 Chrome 不能修复 ID 不匹配。重新绑定后再检查 Engine。升级扩展时保持已解压目录不变；如果更换目录导致 ID 改变，需要重新绑定。以后升级 Engine 安装包也应传入该 ID。

## 无字幕视频

字幕视频只需要主 Engine。需要翻译没有公开字幕的视频时，再双击 `Install No-Caption Whisper.command`；它会通过 Homebrew 安装 whisper.cpp，并下载多语言模型。

## 卸载

双击 `Uninstall LocalTube Dub Engine.command`。卸载器移除 Engine、Native Host 和开机自启，但保留已下载的 Whisper 模型，避免以后重新安装时再次下载。

## 当前发布状态

这是未签名、未公证的私测安装包。macOS 如果阻止首次打开，请右键该 `.command` 文件并选择“打开”。公开发布前仍需要 Apple Developer ID 签名、公证和正式支持地址。
